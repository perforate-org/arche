{
  "scripts": {
    "cleanup": "npx rimraf ./dist",
    "build": "npm run cleanup && trunk build --release"
  },
  "type": "module",
  "dependencies": {
    "@tailwindcss/cli": "^4.0.7",
    "tailwindcss": "^4.0.7"
  }
}
